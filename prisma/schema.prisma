generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql" // MySQL を使用
  url      = env("DATABASE_URL")
}

model User {
  id       Int      @id @default(autoincrement())
  name     String
  email    String   
  password String
  createdAt DateTime @default(now())
}

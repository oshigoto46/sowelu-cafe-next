version: '3.8'
services:
  app:
    build:
      context: .
    volumes:
      - .:/usr/src/app       # ホストとコンテナ間の同期
      - /usr/src/app/node_modules  # node_modules の競合を防ぐ
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: development
      DATABASE_URL: mysql://root:password123@mysql:3306/sowelu_development
    command: ["npm", "run", "dev"] # 開発モードでホットリロード
    depends_on:
      - mysql  # MySQL の起動を待つ
    networks:
      - app-network

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password123
      MYSQL_DATABASE: sowelu_development
    ports:
      - "3306:3306"
    command:
      --default-authentication-plugin=mysql_native_password # 外部アクセス用
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - app-network

networks:
  app-network:
    name: app-network  # 明示的にネットワーク名を指定
    driver: bridge

volumes:
  mysql-data:

